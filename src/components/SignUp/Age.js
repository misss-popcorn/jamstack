const Age = () => {
  return (
    <div className="formField">
      <label>Age</label>
      <input type="text" placeholder="Age in number" autoComplete="off" />
    </div>
  );
};

export default Age;
